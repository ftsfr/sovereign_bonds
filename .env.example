# Bloomberg Terminal must be running for xbbg to work
# No credentials needed in .env - Bloomberg uses local terminal connection

# Set to True to skip the Bloomberg Terminal prompt
BLOOMBERG_TERMINAL_OPEN=False
